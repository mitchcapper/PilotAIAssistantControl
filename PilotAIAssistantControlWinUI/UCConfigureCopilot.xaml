<UserControl x:Class="PilotAIAssistantControl.UCConfigureCopilot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:PilotAIAssistantControl"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="HintText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}"/>
            <Setter Property="IsTextSelectionEnabled" Value="False"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
            <Setter Property="Margin" Value="0,8,0,4"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <!-- Auto-discover checkbox -->
		<CheckBox Name="ChkAutoDiscover" Content="Auto-discover token from local IDEs" IsChecked="{Binding AutoDiscover, Mode=TwoWay}"
                Checked="ChkAutoDiscover_Checked">
			<ToolTipService.ToolTip>
				<ToolTip Content="When checked, the token will be automatically discovered from JetBrains or VS Code Copilot config files"/>
			</ToolTipService.ToolTip>
		</CheckBox>

        <!-- Token file locations info -->
		<Expander Header="{Binding AutoDiscoverResult}"
                  Collapsed="WhereToken_Collapsed" x:Name="expanderAutoDiscoverPathInfo"
                  Visibility="{Binding AutoDiscover,Converter={StaticResource boolNullVisibilityCollapsedConverter}}">
            <TextBlock Name="TxtTokenLocations" TextWrapping="Wrap"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
        </Expander>

        <!-- Sign In button (visible when auto-discover is disabled) -->
        <Button Name="BtnSignIn" Content="🔑 Sign In"
                Click="SignInWithGitHub_Click"
                Visibility="{Binding AutoDiscover,Converter={StaticResource boolNullVisibilityCollapsedConverter}, ConverterParameter=true}">
			<ToolTipService.ToolTip>
				<ToolTip Content="Sign in with GitHub to get a new token"/>
			</ToolTipService.ToolTip>
		</Button>

		<!-- Device Flow Status -->
		<Border Name="DeviceFlowPanel"
                Background="{ThemeResource InfoBarWarningSeverityBackgroundBrush}"
                Style="{StaticResource SectionCard}" Visibility="Collapsed">
			<StackPanel>
				<TextBlock Text="Complete sign-in in your browser:" FontWeight="SemiBold"/>
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="Enter code: "/>
					<TextBlock Name="TxtDeviceCode" FontWeight="Bold"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               FontFamily="Consolas"/>
					<Button Content="📋"
                            Click="CopyDeviceCode_Click">
						<ToolTipService.ToolTip>
							<ToolTip Content="Copy code to clipboard"/>
						</ToolTipService.ToolTip>
					</Button>
				</StackPanel>
				<TextBox IsReadOnly="True" BorderThickness="0" Name="TxtDeviceUrl" />
				<Button Name="BtnCancelSignIn" Content="Cancel" Click="CancelSignIn_Click"
                        HorizontalAlignment="Left"/>
			</StackPanel>
		</Border>
	</StackPanel>
</UserControl>
