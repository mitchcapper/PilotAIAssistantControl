<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="PilotAIAssistantControl.FauxExpander"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition x:Name="HeaderColumn" Width="Auto"/>
			<ColumnDefinition x:Name="ContentColumn" Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition x:Name="HeaderRow" Height="Auto"/>
			<RowDefinition x:Name="ContentRow" Height="*"/>
		</Grid.RowDefinitions>

		<ToggleButton x:Name="ExpandToggle"
		              Grid.Column="0" Grid.Row="0"
		              IsChecked="{x:Bind IsExpanded, Mode=TwoWay}"
		              VerticalAlignment="Stretch"
		              HorizontalAlignment="Stretch"
		              Padding="4"
		              Background="Transparent"
		              BorderThickness="0">
			<ContentPresenter Content="{x:Bind Header, Mode=OneWay}"/>
		</ToggleButton>

		<ContentPresenter x:Name="ContentArea"
		                  Grid.Column="1" Grid.Row="1"
		                  Content="{x:Bind FauxContent, Mode=OneWay}"
		                  HorizontalAlignment="Stretch"
		                  VerticalAlignment="Stretch"
		                  Visibility="{x:Bind IsExpanded, Mode=OneWay}"/>
	</Grid>
</UserControl>
