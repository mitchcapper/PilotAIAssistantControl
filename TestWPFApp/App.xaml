<Application x:Class="TestWPFApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TestWPFApp" xmlns:pia="clr-namespace:PilotAIAssistantControl;assembly=PilotAIAssistantControlWPF"
			 StartupUri="MainWindow.xaml">
	<Application.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/PresentationFramework.Fluent;component/Themes/Fluent.xaml" />
			</ResourceDictionary.MergedDictionaries>
			<pia:BoolNullVisibilityCollapsedConverter x:Key="boolNullVisibilityCollapsedConverter" />

			<Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
				<Setter Property="Margin" Value="3"/>
			</Style>
			<Style TargetType="Button" x:Key="OurAccentButtonStyle" BasedOn="{StaticResource AccentButtonStyle}">
				<Setter Property="Margin" Value="3"/>
			</Style>

			<Style TargetType="CheckBox" BasedOn="{StaticResource DefaultCheckBoxStyle}">
				<Setter Property="MinWidth" Value="50"/>
				<Style.Triggers>
					<Trigger Property="IsEnabled" Value="False">
						<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
					</Trigger>
				</Style.Triggers>
			</Style>

			<Style TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}">
				<Setter Property="Padding" Value="3 2"/>
				<Setter Property="Margin" Value="3"/>
			</Style>

			<Style TargetType="ComboBoxItem" BasedOn="{StaticResource DefaultComboBoxItemStyle}">
				<Setter Property="Padding" Value="3 2"/>
				<Setter Property="Margin" Value="0"/>
			</Style>

			<Style TargetType="Separator" BasedOn="{StaticResource {x:Type Separator}}">
				<Setter Property="Margin" Value="3" />
			</Style>

		</ResourceDictionary>
	</Application.Resources>
</Application>
